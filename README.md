
### Logo Logic (IMPORTANT)
- **Top Nav:** `logo.jpg` - 60x60px, circular, `border-radius:50%`, `mix-blend-mode:multiply`, NO bg color wrapper. Alignment: `flex items-center gap-3` left row - DO NOT CHANGE.
- **Footer:** `logo.jpg` - 90x90px, circular, same pattern, NO bg color wrapper. Alignment: `flex-col items-center justify-center` centered.
- **Hero & Product Cards:** `logo-full.jpg` with `mix-blend-mode:multiply` to remove white bg, NO white block behind.
- **Rule:** No `bg-white` or `bg-black` circles behind logos anymore. Clean transparent look.

### Tech Stack
- Tailwind CSS CDN
- Vanilla JS (Cart in localStorage `bloom_cart`)
- WhatsApp Checkout

### How to Run
1. Keep `logo.jpg` and `logo-full.jpg` in same folder as html
2. Open `index-final-frozen.html` in browser
3. No build step needed

### Cart Functions
- `addToCart(id)`, `updateCart()`, `toggleCart()`, `checkoutWhatsApp()`
- Cart saved in localStorage, survives reload

### Freezing Rule
- This file is FROZEN. For any new changes, copy to `index-v5.html`
- Do not change top alignment
- Do not add color behind logos

### Contact
WhatsApp: +91 8197018680
Free Shipping: Over $40
